import numpy as np
from vector_codes import *
from matrix_codes import *
from qr_decomp import *

def rf_analytic(f, x0: vector, eps: float):
    
    J = matrix(x.size, x.size)
    x = vector.copy(x0)


def rf_num(f, x0: vector, eps: float, )
